using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ChangeSword : MonoBehaviour
{
    // Start is called before the first frame update
    public Sprite nuevaEspada;

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
